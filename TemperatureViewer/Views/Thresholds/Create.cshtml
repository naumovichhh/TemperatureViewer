@model Threshold

@{
    ViewData["Title"] = "Create";
}

<h1>Создать пороги для датчиков</h1>

<form asp-action="Create" asp-anti-forgery="true">
    <div class="validation" asp-validation-summary="ModelOnly"></div>
    <div class="row">
        <div class="col-8 col-md-5 col-lg-4">
            <div class="form-group">
                <label class="form-label">Нижний порог</label>
                <input class="form-control" name="P1" type="number" />
            </div>
            <div class="form-group">
                <label class="form-label">Нижний порог комфорта</label>
                <input class="form-control" name="P2" type="number" />
            </div>
            <div class="form-group">
                <label class="form-label">Верхний порог комфорта</label>
                <input class="form-control" name="P3" type="number" />
            </div>
            <div class="form-group">
                <label class="form-label">Верхний порог</label>
                <input class="form-control" name="P4" type="number" />
            </div>
            <div class="sensors-checkboxes-group">
                @{
                    int i = 0;
                }
                @foreach (var sensor in ViewBag.Sensors)
                {
                    <div class="form-group">
                        <div class="row">
                            <label class="form-label col-11">@sensor.Name</label>
                            <input class="input-checkbox col-1" type="checkbox" name="Sensors[@i].Id" value="@sensor.Id"/>
                        </div>
                    </div>
                    {
                        i++;
                    }
                }
            </div>
            <div class="form-group">
                <input class="btn btn-primary" type="submit" value="Сохранить"/>
            </div>
        </div>
    </div>
</form>